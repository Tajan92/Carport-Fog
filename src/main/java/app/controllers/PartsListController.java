package app.controllers;

import app.services.ServiceFactory;
import io.javalin.Javalin;

public class PartsListController {

//    public void addRoutes(Javalin app, ServiceFactory serviceFactory){
//        app.post("/createPartsList", ctx -> createPartsList(ctx, serviceFactory));
//        app.get("/getPartsList", ctx -> getPartsList(ctx, serviceFactory));
//        app.post("/updatePartsList", ctx -> updatePartsList(ctx, serviceFactory));
//        app.post("/deletePartsList", ctx -> deletePartsList(ctx, serviceFactory));
//    }
}
